<div class="hero">
  <p class="large brandon">Jewelry as unique as you are.</p>
  <p class="small brandon">We’re launching a hi-tech charm bracelet &amp; app for your life’s most meaningful moments..</p>
</div>

<div class="form-wrap clearfix <%= "ended" if Rails.application.config.ended %>">
  <% if Rails.application.config.ended %>
    <%= render "users/campaign_ended" %>
  <% else %>
    <%= render "users/campaign_ongoing" %>
  <% end %>
</div>